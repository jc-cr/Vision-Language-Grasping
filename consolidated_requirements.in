# Main project requirements
-r requirements.txt
# Graspnet requirements
-r models/graspnet/requirements.txt

# Specific torch version for CUDA 11.1
torch==1.10.1+cu111
torchvision==0.11.2+cu111
torchaudio==0.10.1

# Keep numpy at a version compatible with other dependencies
numpy>=1.19.4,<1.20